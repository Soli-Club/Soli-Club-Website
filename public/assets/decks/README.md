# Deck Assets

`deck-placeholder.svg` is a simple pixel-style deck base for Step 1. A final turntable/decks illustration can replace it later as long as the transparent canvas keeps a similar wide aspect ratio.
