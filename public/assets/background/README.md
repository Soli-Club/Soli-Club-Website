# Background Assets

The night-sky component already creates stars in CSS. Replace these placeholder SVGs with the client's poster-style artwork when it is ready:

- `moon-placeholder.svg`
- `cloud-1-placeholder.svg`
- `cloud-2-placeholder.svg`

Keep the files transparent, with generous empty space around soft glow edges. The layout reads them from `/assets/background/`.
